% This file documents AI-assisted interactions for ME 523 HW7.
% Include in a parent .tex file with: \input{prompts}

\section*{AI Use Documentation}

\subsection*{Interaction 1}

\textbf{Prompt:}
\begin{quote}
Set up the C++ project structure for a 2D incompressible Navier-Stokes solver using Kokkos and Trilinos. Use the Chorin projection method on a staggered MAC grid. Organize as a hybrid library with header-only templated code and compiled source files for non-templated components (pressure solver, CSV output, physics kernels). Include Spack-based dependency management, CMake build system, and Doxygen documentation.
\end{quote}

\textbf{Response summary:}
Claude scaffolded the full project: \texttt{include/} headers for the solver pipeline (grid, boundary conditions, initial conditions, integrators, physics, pressure solver, output, and a templated \texttt{Solver<BC, Integrator>}), compiled \texttt{src/} sources for \texttt{PressureSolver}, \texttt{CSVOutput}, and \texttt{physics::} functions, Spack environment definitions for multiple machines, CMake build files, GitHub Actions CI for Doxygen deployment, and the main driver in \texttt{apps/ns\_solver.cpp}. Function stubs with TODOs were left for the actual numerical implementations.

\textbf{Date and Model:} Claude Opus 4.6, 2026-04-12

\subsection*{Interaction 2}

\textbf{Prompt:}
\begin{quote}
Look at the solver files, the physics files etc to check what is causing the error:
\texttt{Kokkos::View ERROR: attempt to access inaccessible memory space (label="ke\_history")}.
\end{quote}

\textbf{Response summary:}
Claude identified that the \texttt{ke\_history} and \texttt{div\_history} views in \texttt{SimState} were declared as \texttt{Kokkos::View<double*>} with no explicit memory space. Because Kokkos is built with CUDA as the default execution space on this machine (\texttt{ncam} spack environment: \texttt{kokkos +cuda cuda\_arch=120}), the default memory space is \texttt{CudaSpace}. Host code in \texttt{solver.hpp} was then trying to directly index these device views after \texttt{parallel\_reduce} returned a host scalar, which Kokkos correctly rejected. The fix was to declare both views as \texttt{Kokkos::View<double*, Kokkos::HostSpace>} and remove the now-redundant \texttt{create\_mirror\_view\_and\_copy} calls in \texttt{output.cpp}.

\textbf{Date and Model:} Claude Sonnet 4.6, 2026-04-13

\subsection*{Interaction 3}

\textbf{Prompt:}
\begin{quote}
Why do the last tests for KE and divergence both fail? Is there something wrong with how it is implemented? Is it an issue with the test or the source code?
\end{quote}

\textbf{Response summary:}
Claude identified two separate bugs introduced by the partner's PR on the \texttt{diagnostics} branch.
(1) \textbf{KE test stale}: Commit \texttt{f77dcf9} correctly added area-weighting (\texttt{* dx * dy}) to \texttt{compute\_kinetic\_energy} to compute the domain-integrated kinetic energy, but the \texttt{test\_uniform\_field} expected value (130.0) was never updated. Claude updated the expected value to \texttt{130.0 * grid.dx * grid.dy} with an explanatory comment.
(2) \textbf{Divergence normalization wrong}: \texttt{compute\_l2\_divergence} divided \texttt{sum\_div\^{}2} by \texttt{nx*ny} before taking the square root, yielding an RMS rather than an L2 norm. \texttt{test\_known\_divergence} expected the true L2 norm \texttt{sqrt(sum div\^{}2)}. Claude removed the division. Both fixes were committed to a new branch \texttt{div-ke-correction} and submitted as PR \#73.

\textbf{Date and Model:} Claude Sonnet 4.6, 2026-04-13

\subsection*{Interaction 4}

\textbf{Prompt:}
\begin{quote}
Look at the current setup in test and physics for computing divergence. I want this \texttt{l2\_div} to mean RMS for mesh refinement purposes. Recall the branch we made \texttt{div-ke-correction}, fix it to be consistent with this version we had switched it to a true L2 norm. Then build and run all tests and the solver app to ensure it works.
\end{quote}

\textbf{Response summary:}
Claude compared the \texttt{diagnostics} branch (RMS: \texttt{sqrt(sum\_div\^{}2 / (nx*ny))}, expected 2.0) against \texttt{div-ke-correction} (true L2: \texttt{sqrt(sum\_div\^{}2)}, expected \texttt{2*sqrt(nx*ny)}). On \texttt{div-ke-correction}, the implementation used the true L2 but the test expected value had already been partially updated to the RMS value with leftover contradictory comments. Claude fixed \texttt{src/physics.cpp} to divide by \texttt{nx*ny} before the square root and cleaned up the comment in \texttt{tests/test\_l2\_divergence.cpp}. Build succeeded and all 3 tests (OutputTest, KineticEnergyTest, L2DivergenceTest) passed. The PR \#73 description was updated to clarify that \texttt{compute\_l2\_divergence} returns an RMS (grid-size-independent metric suitable for mesh refinement studies), not a true L2 norm.

\textbf{Date and Model:} Claude Sonnet 4.6, 2026-04-13
